// Import the `next-intl` plugin
const withNextIntl = require('next-intl/plugin')();

// Export the configuration by merging with the `next-intl` plugin configuration
module.exports = withNextIntl();
